## Do

Prepare everything for the external resource (config, scripts, docs) up to the point a human must
run the privileged/credentialed step.

## Done when

Everything around the manual step is ready and documented; the agent records failed:blocked
(needs-human) — the human performs the one-time provisioning step.
