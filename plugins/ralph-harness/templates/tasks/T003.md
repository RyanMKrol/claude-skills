## Do

Implement the first real feature using the T002 interface. (If a `design` doc is referenced, read
and follow it.)

## Done when

Feature works, unit + integration tests pass, README status row flipped.
