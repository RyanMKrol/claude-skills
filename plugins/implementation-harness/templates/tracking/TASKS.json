{
  "_doc": "Implementation backlog for the single sequential Ralph loop. SOURCE OF TRUTH for done/not-done + dependency order; docs/HARNESS.md §8.1 = schema. Each task's `do` + `done-when` live in a per-task Markdown spec at the `spec` path (.harness/tasks/TNNN.md, sections '## Do' / '## Done when'); every BUILDABLE task carries `facets` (difficulty auto-tuning) — a needs-human gated task is carved out and carries none. `gate` is `null` (buildable) or `needs-human` (a one-time human step). To require a human to review a deliverable BEFORE dependents proceed, author the work as a normal buildable task plus a separate `needs-human` review task that dependsOn it, and point the dependents at the review task. These are ILLUSTRATIVE EXAMPLES showing the schema, the needs-human gate, the spec-file convention, and facets — REPLACE them (and their tasks/*.md specs) with your own atomic, dependency-ordered tasks before running the loop.",
  "version": 1,
  "tasks": [
    {
      "id": "T001",
      "title": "Project scaffold + CI green on an empty build",
      "status": "pending",
      "dependsOn": [],
      "gate": null,
      "tags": ["scaffold"],
      "scope": ["project manifest", "src/ entrypoint", ".github/workflows/ci.yml"],
      "design": null,
      "verify": [],
      "spec": ".harness/tasks/T001.md",
      "facets": { "layer": "infra", "workType": "config", "risk": [] }
    },
    {
      "id": "T002",
      "title": "Core module skeleton + public interface",
      "status": "pending",
      "dependsOn": ["T001"],
      "gate": null,
      "tags": ["coding"],
      "scope": ["src/core.*", "tests/core_*"],
      "design": null,
      "verify": [],
      "spec": ".harness/tasks/T002.md",
      "facets": { "layer": "backend", "workType": "feature", "risk": ["touches-core"] }
    },
    {
      "id": "T003",
      "title": "First feature against the core interface",
      "status": "pending",
      "dependsOn": ["T002"],
      "gate": null,
      "tags": ["coding"],
      "scope": ["src/feature_x.*", "tests/feature_x_*"],
      "design": ".harness/docs/designs/T003-feature-x.md",
      "verify": [],
      "spec": ".harness/tasks/T003.md",
      "facets": { "layer": "backend", "workType": "feature", "risk": [] }
    },
    {
      "id": "T004",
      "title": "Integration test harness",
      "status": "pending",
      "dependsOn": ["T003"],
      "gate": null,
      "tags": ["testing"],
      "scope": ["tests/integration/*"],
      "design": null,
      "verify": ["run-app"],
      "spec": ".harness/tasks/T004.md",
      "facets": { "layer": "backend", "workType": "feature", "risk": [] }
    },
    {
      "id": "T005",
      "title": "Provision external resource",
      "status": "pending",
      "dependsOn": ["T004"],
      "gate": "needs-human",
      "tags": ["infra"],
      "scope": ["infra/*", "docs/SETUP.md"],
      "design": null,
      "verify": [],
      "spec": ".harness/tasks/T005.md"
    }
  ]
}
