## Inbox

<!-- Captured via /implementation-harness-capture-idea (or hand-edit). Zero ceremony — one numbered
     bullet per idea. Swept into real TASKS.json tasks via /implementation-harness-convert-ideas,
     which removes each converted bullet. This file IS committed, so the inbox travels with the repo;
     if a project would rather keep raw ideas private, add `.harness/tracking/IDEAS.md` to .gitignore. -->
