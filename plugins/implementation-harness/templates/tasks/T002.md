## Do

Define the central module's public interface (types/traits/functions) that later tasks build
against, with stub implementations and unit tests for the contract.

## Done when

The interface compiles, is unit-tested, and is documented; downstream tasks can code against it
without touching its internals.
